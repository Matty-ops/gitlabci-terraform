# Partie 1 : Pipeline de Base "Hello World"

## 🎯 Objectif
S'assurer que l'environnement GitLab CI/CD est opérationnel et que le Runner peut exécuter un script simple.

## 📝 Consignes
1. Définir un `stage` unique nommé `build`.
2. Créer un `job` nommé `hello_world_job`.
3. Utiliser une image simple (ex: `alpine:latest`).
4. Dans la section `script`, afficher le message **"Hello World, la CI fonctionne !"**.

## 💡 Résultat Attendu
Une pipeline avec un seul stage et un seul job qui passe avec succès.
